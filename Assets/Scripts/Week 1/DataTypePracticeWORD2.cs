using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataTypePracticeWORD2 : MonoBehaviour
{
//More practice with the word assignment, im gonna be so good at writing words in code its gonna be crazy
//people will look at me and say "wow kyler you're so good at coding its insane'
//hey i understand why people have so much fun putting secrets in their code I could write a whole novel in here

//anyways


    public char LetterOne = 'e';
    public char LetterTwo = 'v';
    public char LetterThree  = 'r';
    public char LetterFour = 'n';
    public char LetterFive = 'c';

//okay good I did good this is starting to make sense. ish


    // Start is called before the first frame update
    void Start()
    {
        //round two lets go 
        Debug.Log("Ah, so the name of your home kingdom is: " + LetterOne + LetterTwo + LetterOne + LetterThree + LetterOne + LetterFour + LetterFive +LetterOne);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
