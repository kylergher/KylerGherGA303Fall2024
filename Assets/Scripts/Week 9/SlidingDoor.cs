using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SlidingDoor : DoorScriptWrow //inheriting from DoorScriptWrow also means that we inherit monobehavior, as well as start and update
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
